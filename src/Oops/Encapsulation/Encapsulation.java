package Oops.Encapsulation;

public class Encapsulation {
    // Encapsulation means wrapping up the implementations of the data memberw and methods in a class

    // It will hide the entire code or data into a entity, so that, that can be protected from the outisde world.
}
